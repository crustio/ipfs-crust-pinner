name: Test ipfs-crust-action
on:
  # mannual trigger
  workflow_dispatch:

jobs:
  pin_on_crust:
    name: test ipfs-crust-action
    runs-on: ubuntu-latest
    steps:
      - name: Pin to Crust
        id: pintocrust
        uses: crustio/ipfs-crust-action@v2.0.6
        continue-on-error: true
        with:
          cid: QmYqG5k58ohv5enm6era7Cp7LxmoxDq2iYtcmMohzEjm6y
          seeds: ${{ secrets.CRUST_SEEDS }}
